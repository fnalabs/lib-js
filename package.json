{
  "name": "lib-js",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "document": "npm run document --workspaces --if-present",
    "fix": "npm run fix --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "release": "npm run release --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fnalabs/lib-js.git"
  },
  "bugs": {
    "url": "https://github.com/fnalabs/lib-js/issues"
  },
  "homepage": "https://github.com/fnalabs/lib-js#readme",
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "18.16.0"
  }
}
